import { mount as mountProducts } from "products/ProductIndex";
import { mount as mountCart } from "cart/CartIndex";

// const elProducts = document.querySelector("#dev-products");
// if (elProducts) mountProducts(elProducts);

// const elCart = document.querySelector("#dev-cart2");
// if (elCart) mountCart(elCart);

console.log("app shell");
